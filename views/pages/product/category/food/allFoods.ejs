<%- include('../../../../partials/header.ejs') %>

<div class="container">
  <div class="row">
    <div class="col-3"></div>
    <div class="col-8">
      <div class="card">
        <div class="card-body">
          <h4 class="text-center text-secondary">All food products</h4>
        </div>
        <div class="card-body">
          <div class="row">
            <% foods.forEach(food => { %>
              <div class="col-4">
                <div class="card">
                  <% let productThumnail; %> 
                  <% for( let thumbnail = 0; thumbnail < food.itemimg.length; thumbnail++ ) { %>
                    <% productThumnail = food.itemimg[0] %> 
                  <% } %>
                  <img src="<%= productThumnail %> " class="card-img-top mx-auto" style="width: 150px; height: auto;">
                  <div class="card-body">
                    <h5 class="card-title"><%= food.itemname %> </h5>
                    <p><span>Price:</span>&nbsp;<%= food.price %> </p>
                    <a href="/product/food/<%= food._id %>" class="card-link">view details</a>
                  </div>
                </div>
              </div>
             <% }) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

</body>
</html>